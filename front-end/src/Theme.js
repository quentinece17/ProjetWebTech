import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#44566c',
    },
    primary: {
      main: '#FFFFFF',
    },
    secondary: {
      main: '#2f435e',
    },
  },
});

export default theme;
