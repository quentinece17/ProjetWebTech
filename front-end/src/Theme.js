import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#44566c',
    },
    primary: {
      main: '#44566c',
    },
    secondary: {
      main: '#2f435e',
    },
  },
});

export default theme;
